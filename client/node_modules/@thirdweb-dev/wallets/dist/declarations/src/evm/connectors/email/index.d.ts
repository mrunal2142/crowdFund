import { ConnectParams, TWConnector } from "../../interfaces/tw-connector";
import { EmailWalletConnectionArgs, EmailWalletConnectorOptions } from "./types";
import { Chains } from "@paperxyz/embedded-wallet-service-sdk";
import { ethers, Signer } from "ethers";
export declare const PaperChainMap: Record<number, Chains>;
export declare class EmailWalletConnector extends TWConnector<EmailWalletConnectionArgs> {
    readonly id: string;
    readonly name: string;
    ready: boolean;
    private user;
    private paper;
    constructor(options: EmailWalletConnectorOptions);
    connect(args?: ConnectParams<EmailWalletConnectionArgs>): Promise<string>;
    disconnect(): Promise<void>;
    getAddress(): Promise<string>;
    isConnected(): Promise<boolean>;
    getProvider(): Promise<ethers.providers.Provider>;
    getSigner(): Promise<Signer>;
    isAuthorized(): Promise<boolean>;
    switchChain(chainId: number): Promise<void>;
    private getUser;
}
//# sourceMappingURL=index.d.ts.map