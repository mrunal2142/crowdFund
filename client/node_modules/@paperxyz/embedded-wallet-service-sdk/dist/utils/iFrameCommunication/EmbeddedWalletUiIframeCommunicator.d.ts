import { StyleObject } from "../../interfaces/Modal";
import { CustomizationOptionsType } from "../../interfaces/utils/IframeCommunicator";
import { IframeCommunicator } from "./IframeCommunicator";
export declare class EmbeddedWalletUiIframeCommunicator<T extends {
    [key: string]: any;
}> extends IframeCommunicator<T> {
    protected clientId: string;
    constructor({ clientId, path, container, customizationOptions, iframeStyles, onIframeInitialize, }: {
        clientId: string;
        path: string;
        container: HTMLElement;
        iframeStyles?: StyleObject;
        customizationOptions?: CustomizationOptionsType;
        onIframeInitialize?: () => void;
    });
    onIframeLoadedInitVariables(): Promise<{
        authCookie: string | null;
        deviceShareStored: string | null;
        clientId: string;
    }>;
}
export declare const EMBEDDED_WALLET_MODAL_UI_IFRAME_ID = "paper-embedded-wallet-modal-iframe";
